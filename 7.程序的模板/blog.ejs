<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  <% entries.map(entry => { %>
    <div><%= entry.title %></div>
    <div><%= entry.date %></div>
    <div><%= entry.body %></div>
  <% }) %>
</body>

</html>